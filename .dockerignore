node_modules
web/dist
.git
.env
test
docs
.claude
.playwright-mcp
graphify-out
# NOTE: do NOT ignore `drizzle/` — the image runs `drizzle-kit migrate` on start,
# which needs the migration files. Excluding it leaves the DB with no tables.
